\* ML-DSA pack_codes/unpack_codes and ML-KEM encode/decode_compressed:
\* real widths with n = 256, plus all widths with 1..9 codes; compress,
\* decompress, encode_12/decode_12 are checked by ASSUMEs
CONSTANTS
  Configs <- AllConfigs
SPECIFICATION Spec
INVARIANTS
  NoFault
  AccRange
  BytesWritten
  PackIsFIPS
  UnpackIsFIPS
  RoundTrips
  StepBound
CHECK_DEADLOCK TRUE
