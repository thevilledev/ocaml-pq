\* SLH-DSA digest split + hypertree traversal: the six FIPS 205 (h, d, a, k, m)
\* combinations (shared by the SHA2 and SHAKE variants) and all synthetic
\* (h', d) with h' <= 17 and tree_bits = h'(d-1) <= 64
CONSTANTS
  Params <- AllParams
SPECIFICATION Spec
INVARIANTS
  NoFault
  ForsAddressOK
  SignLayersOK
  VerifyLayersOK
  TopTreeZero
  VerifyAccepts
  TreeAddressBytes
  StepBound
CHECK_DEADLOCK TRUE
